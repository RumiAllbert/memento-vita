---
import Layout from '../layouts/Layout.astro';
import WhatIfSimulator from '../components/WhatIfSimulator.tsx';
---

<Layout
  title="What If Simulator — Memento Vita | Life Scenario Calculator"
  description="What if you retired earlier? Spent less time on your phone? Visited your parents more? Use the What If simulator to see how different choices change your remaining weeks."
>
  <WhatIfSimulator client:load />
  <script>
    if (typeof window !== 'undefined' && localStorage.getItem('onboarded') !== 'true') {
      window.location.href = '/';
    }
  </script>
</Layout>
